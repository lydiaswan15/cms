import { UsersDirectiveDirective } from './users-directive.directive';

describe('UsersDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UsersDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
